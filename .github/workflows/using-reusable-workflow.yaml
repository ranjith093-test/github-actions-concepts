name: Calling Reusable Workflows
on:
  workflow_dispatch:
    inputs:
      name:
        description: 'Input Description'
        type: string
        default: 'Ranjith'
        required: false

jobs:
  calling-a-reusable-wf:
    uses: ranjith093-test/workflows/.github/workflows/reusable.yaml@main
    with:
      name: ${{ inputs.name }}
    secrets:
      access-token: ${{ secrets.ACCESS_TOKEN }}
